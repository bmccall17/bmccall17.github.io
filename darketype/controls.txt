  Where the controls are for adjustments — everything is in /home/bam/projects/decay/index.html, lines 80-131,
  in the CONFIG object at the very top of the <script> block. 
  
  Here's what you can tune:
    Physics: gravity, drag, viscosity, lateralJitter

  Decay timing: decayStartDelay (seconds before melt starts), decayDuration (full melt duration)

  Surface tension: stickChance, stickFramesMin/Max (particles briefly pause before falling)

  Visual: pixelClusterSize (particle resolution), trailFade (afterglow persistence), metaballRadius,
  puddleWaveSpeed/Amp

  Interaction: mouseRadius, mousePushForce, mouseDecayBoost, collectionRadius/Strength/SampleRate

  Audio: masterVolume, droneVolume, droneBaseFreq, dripVolume, dripMinInterval, dripChance

  One object, one file, every knob exposed. Change a value, refresh, see the difference.

--- LOCAL screenshot grab ---

❯ and finally, tell me what it would take to use the MOST RECENT saved screenshot as the initial background
image. probably will need to know exactly where the screenshots are saved locally when the website loads in.
but ill let you figure out the logic here.

● Good question. Let me figure out the pieces first.

  Read 2 files (ctrl+o to expand)

● Found them. Screenshots live at /mnt/c/Users/Brett A McCall/Pictures/Screenshots/ (Windows Win+PrintScreen
  default). Here's the situation:

  The core problem: The browser can't read your local filesystem. The app is served via npx serve, which only
  serves files from the project directory. So the browser has no way to reach C:\Users\Brett A
  McCall\Pictures\Screenshots\ directly.

  Two approaches:

  Option A: Launch script (simplest)

  A small bash script that copies the latest screenshot to hero.png and then starts the server. One command,
  always fresh.

  #!/bin/bash
  # launch.sh — copy latest screenshot, start server
  SCREENSHOTS="/mnt/c/Users/Brett A McCall/Pictures/Screenshots"
  LATEST=$(ls -t "$SCREENSHOTS"/*.png 2>/dev/null | head -1)
  cp "$LATEST" /home/bam/projects/decay/hero.png
  npx serve /home/bam/projects/decay

  You'd run bash launch.sh instead of npx serve. The screenshot is baked in at launch time.

  Option B: Tiny custom server (live, always latest)

  Replace npx serve with a ~30-line Node.js server that serves the project directory AND exposes a
  /latest-screenshot endpoint. The endpoint reads the screenshots folder, finds the newest .png, and streams it
  back. The index.html points heroImageUrl at /latest-screenshot instead of ./hero.png. Every time you reload,
  it grabs the freshest screenshot — no manual copy.

  This is the one that makes the prank work perfectly: take a screenshot of someone's desktop, open the page,
  fullscreen, walk away. Their screen melts.

  ---
  Option B is the better UX but requires a small server.js file. Option A is zero new files but requires
  remembering to re-run the script if the screenshot changes. Which direction do you want?