# v0.1.8 - 📚 series, seo, and the system that publishes itself

**released:** may 9, 2026

## summary
five-post agent828 technical series fully published, Hashnode series infrastructure built and wired, SEO metadata automated end-to-end, and the weblog index upgraded with clickable series filters. the system now knows what it is, what series it belongs to, and how to explain itself to a search engine — without anyone touching a dashboard.

---

## changes

### new content — agent828 build arc (5 posts)
- added `2026-04-29_make_the_system_explain_itself.md` — architecture knowledge graph + live status narration
- added `2026-04-30_the_crm_that_drafts_its_own_followups.md` — AE workbench: meetings → analysis → drafts → approval gate
- added `2026-05-03_the_honest_column.md` — `follow_up_date` as derived fact, not stored guess
- added `2026-05-08_how_we_stopped_our_content_engine_from_repeating_itself.md` — 4 anti-repetition guardrails
- added `2026-05-09_the_backlog_is_a_feature.md` — BACKLOG.md as trigger-based methodology, not a priority queue

### hashnode series infrastructure
- created 3 Hashnode series: `darketype-devlog`, `crm-agent828`, `agent828-build-arc`
- reassigned series frontmatter across all 42 entries: 22 devlog, 5 agent828-build-arc, 4 crm-agent828, 11 previously untagged
- moved `the-crm-i-built-by-being-the-customer` and `the-persistence-polarity` from devlog → crm-agent828
- moved `the-context-lake` and `understanding-the-lake` from devlog → agent828-build-arc

### SEO automation (end-to-end, zero dashboard work)
- `build_hashnode.js` — added `extractSeoDescription()` to pull first 150 chars of body (markdown stripped)
- `build_hashnode.js` — now writes `seo_title`, `seo_description`, `og_image` to every `.hashnode/` proxy file
- `hashnode_api_sync.js` — now pushes `metaTags { title, description, image }` via API on every sync
- API schema confirmed via GraphQL introspection (`__type(name: "MetaTagsInput")`) — `seo`/`ogMetaData` are wrong, `metaTags` is correct

### publishable states expanded
- `build_hashnode.js` — PUBLISHABLE_STATES expanded from `['shipped', 'expanded', 'learning']` to include `mess`, `frustrated`, `optimistic`, `seed`, `debugging`, `broken`
- 11 previously-excluded entries now published to Hashnode: daemon summons, scorecard gate, leuchtturm, port ocean pain, runbooks, dynamic chaos, optimism & TED, the origin story, port.io + vertex, repoptics meets vertex, singlarity
- only `void` remains excluded (placeholder entries)

### weblog index — series filter
- `build_weblog.js` — `post` object now includes `series` (short label: devlog/crm/agent828), exposed in `entries.json`
- `build_weblog.js` — static `<li>` rows now include `data-series` attribute and `<span class="series-tag">[label]</span>`
- `weblog/index.html` — `.series-tag` styled as clickable (cursor:pointer, underline on hover, glow when active)
- `weblog/index.html` — `applySeriesFilter()` / `clearSeriesFilter()` functions added
- `weblog/index.html` — `#series-filter-bar` bar appears above list when filter active, with `[× clear]` dismiss
- `weblog/index.html` — `?series=` URL param triggers filter on load (bookmarkable deep links)
- `weblog/index.html` — dynamically rendered list (from `entries.json`) also respects filter and re-applies after re-render

### entry page — series tag & hashnode link
- `entry.html` — after `hashnode ↗` link, a `[crm]` / `[agent828]` / `[devlog]` pill renders from `meta.series`
- `entry.html` — series pill links to `https://darketype.hashnode.dev/series/<slug>` in new tab
- series link only renders when the manifest fetch succeeds (graceful degradation maintained)

### images — 9 custom tactical UI mockups
- added 9 generated images across 5 entries: meetings panel, AE brief panel, pending drafts banner, activity timeline, arch graph status, attention banner, arch accordion degraded, content queue diverse, entity cooldown log, accounts next touch, backlog trigger, orchestrator output
- all images stored at `darketype/entries/media/<entry-slug>/` with correct `../entries/media/` relative paths
- `build_hashnode.js` — `resolveMediaPaths()` converts to absolute GitHub Pages URLs for Hashnode sync

### /entry workflow updated
- documented all 9 publishable states + void
- documented 3 active series slugs with descriptions and Hashnode URLs
- documented image path convention (`../entries/media/<slug>/`) with rationale
- documented SEO auto-generation (seo_title, seo_description, og_image)
- updated step 4 confirm to check series filter

---

## files changed

### new files
- `darketype/entries/2026-04-29_make_the_system_explain_itself.md`
- `darketype/entries/2026-04-30_the_crm_that_drafts_its_own_followups.md`
- `darketype/entries/2026-05-03_the_honest_column.md`
- `darketype/entries/2026-05-08_how_we_stopped_our_content_engine_from_repeating_itself.md`
- `darketype/entries/2026-05-09_the_backlog_is_a_feature.md`
- `darketype/entries/media/2026-04-30_crm_followups/` — meetings panel, AE brief, pending drafts, activity timeline
- `darketype/entries/media/2026-04-29_arch_graph/` — arch graph status, attention banner, arch accordion degraded
- `darketype/entries/media/2026-05-08_content_engine/` — content queue diverse, entity cooldown log
- `darketype/entries/media/2026-05-03_honest_column/` — accounts next touch, backlog trigger
- `darketype/entries/media/2026-05-09_backlog_feature/` — orchestrator output
- `devnotes/releases/v0.1.8.md`
- 11 new `.hashnode/` proxy files for previously-excluded entries

### modified files
- `scripts/build_weblog.js` — series label map, series field in post object, series-tag in `<li>` render
- `scripts/build_hashnode.js` — extractSeoDescription(), seo_title/description/og_image in proxy frontmatter, expanded PUBLISHABLE_STATES
- `scripts/hashnode_api_sync.js` — metaTags in publishPost and updatePost inputs
- `darketype/entry.html` — series tag link after hashnode ↗ link
- `darketype/weblog/index.html` — series-tag CSS, filter bar, applySeriesFilter/clearSeriesFilter, URL param support, data-series on `<li>`
- `.agent/workflows/entry.md` — full rewrite with states, series slugs, image paths, SEO auto-generation
- 6 entry frontmatter files — series field updated to correct series slug
- all `.hashnode/` proxy files — regenerated with seo_title, seo_description, og_image fields

---

## verification
- [x] checked `darketype/weblog` — series tags render and filter works
- [x] checked all 3 Hashnode series — populated and correct
- [x] checked SEO fields on Hashnode — title, description, OG image pushed via API
- [x] checked hashnode links on previously-excluded posts — now published
- [x] checked `/entry` workflow — updated and accurate
